<h1>Task: <%= @task.title %></h1>

<h2>How to do: <%= @task.details %></h2>
<p>Completed: <%= @task.completed %></p>
<%= link_to 'Back to all tasks', tasks_path %>

<%= link_to 'Delete',
task_path(@task),
data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
